{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/RejectReasonMisleading",
  "title": "RejectReasonMisleading",
  "type": "object",
  "properties": {
    "reason": {
      "description": "rejected due to false or misleading content",
      "type": "string",
      "enum": [
        "false_or_misleading"
      ]
    }
  },
  "required": [
    "reason"
  ]
}